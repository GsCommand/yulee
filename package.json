{
  "name": "yulee-paver-sealing-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp *.html styles.css robots.txt sitemap.xml llms.txt favicon.ico *.webp hydroseal-full-calculator.js public/ && node embed-hydroseal-full-calculator.js && node add-driveway-calculator-image.js",
    "start": "vercel dev"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
