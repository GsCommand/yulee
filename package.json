{
  "name": "yulee-paver-sealing-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public public/images && cp *.html styles.css nocatee-calculator.css robots.txt sitemap.xml llms.txt favicon.ico *.webp public/ && cp images/* public/images/ && node add-driveway-image.js && node add-pool-deck-slider.js && node install-nocatee-calculator.js && node localize-yulee-images.js && node update-homepage-faq.js && node install-elfsight-ai-chatbot.js",
    "start": "vercel dev"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
