{
  "name": "yulee-paver-sealing-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp *.html styles.css robots.txt sitemap.xml llms.txt favicon.ico public/",
    "start": "vercel dev"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
